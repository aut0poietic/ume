import { ComponentInterface } from '../../stencil-public-runtime';
export declare class UmeLayout implements ComponentInterface {
    /**
     * If `true` spans the full width of it's container.
     */
    fluid: boolean;
    render(): any;
}
